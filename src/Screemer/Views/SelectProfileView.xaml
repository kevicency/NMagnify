<UserControl x:Class="Screemer.Views.SelectProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ComboBox DisplayMemberPath="Name"
                  ItemsSource="{Binding AvailableProfiles}"
                  SelectedItem="{Binding ActiveProfile}" />
        <StackPanel Grid.Row="1"
                    Margin="{StaticResource ContentElementMargin}"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Content="{StaticResource appbar_add}" Style="{DynamicResource MetroCircleButtonStyle}" />
            <Button Content="{StaticResource appbar_edit}" Style="{DynamicResource MetroCircleButtonStyle}" />
            <Button Content="{StaticResource appbar_delete}" Style="{DynamicResource MetroCircleButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
